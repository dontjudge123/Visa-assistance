{
  "name": "Visa-assistance Dev",
  "postCreateCommand": "npm install && (cd frontend && npm install) && (cd backend && npm install)",
  "postStartCommand": "npx concurrently \"cd frontend && npm start\" \"cd backend && node server.js\"",
  "forwardPorts": [3000, 5000],
  "portsAttributes": {
    "3000": {
      "label": "Frontend",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  }
}